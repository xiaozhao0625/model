# Master API

P7 Master API control plane for app management, run lifecycle, worker registration, upload flow orchestration, and Model Gateway proxying.

This stage does not execute workers, capture screenshots, run behavior packs, start real infrastructure, or perform model inference.
